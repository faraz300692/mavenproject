steps:
  - label: ":hammer: Tests"
    command: mvn compiler:compile
    branches: "master"
    agents:
        queue: "medium"

  - wait

  - label: ":package: Package"
    command: mvn package
    branches: "master"
    agents:
        queue: "medium"

  - wait

  - label: ":debian: Install"
    command: mvn install
    branches: "master"
    agents:
        queue: "medium"

